// Placeholder smoke test.
//
// The default counter test was removed in Chunk 7 when the app root was
// replaced. Meaningful widget tests will be added alongside real features.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
